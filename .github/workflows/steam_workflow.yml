name: SteamDB Direct Link Notifier

on:
  schedule:
    - cron: "0 22 * * *"   # UTC 22:00 = KST 07:00
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Send SteamDB link to Discord
        run: |
          curl -X POST \
            -H "Content-Type: application/json" \
            -d '{
              "content": "📊 SteamDB Charts 바로 열기\nhttps://steamdb.info/app/3321460/charts/"
            }' \
            ${{ secrets.DISCORD_WEBHOOK }}
